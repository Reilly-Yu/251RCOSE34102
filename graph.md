```mermaid
graph TD
    A[사용자 인터페이스] -- 프로세스 수 입력 --> B(프로세스 생성 모듈);
    B -- 생성된 프로세스 정보 --> C{"프로세스 리스트 (원본)"};
    C -- 복사본 전달 --> D[큐 초기화 & 스케줄러 설정 모듈];
    D -- ready_queue, waiting_queue 생성 --> E(Ready Queue);
    D -- (미사용) --> F(Waiting Queue);
    E -- 프로세스 요청 --> G[CPU 스케줄러 모듈];
    G -- CPU 할당 및 시간 경과 시뮬레이션 --> H[CPU 시뮬레이션 환경];
    H -- 완료/선점/재할당 --> E;
    G -- 완료된 프로세스 정보 --> I[성능 지표 계산 모듈];
    I -- 계산된 지표 --> J[결과 출력 모듈];
    J -- 최종 결과 출력 --> A;
    C -- 원본 데이터 참조 (평가) --> I;